<%= select  :fees_submission, :dates_id, @dates.map { |e| [e.name, e.id]},
            {:prompt => "#{t('select')} #{t('fee_collection')}"},
            {:onChange => "#{remote_function( :url =>
                                                      {:action => "render_collection_assign_form"},
                                              :with => "'id='+value+",
                                              :before => "Element.show('loader1')",
                                              :success => "Element.hide('loader1')")}"
            } %>